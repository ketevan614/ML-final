# =============================================================================
# CORE environment  ->  install into .venv
# Everything EXCEPT the deep-learning stack (torch / neuralforecast), which
# conflicts on resolution and lives in its own env (see requirements-dl.txt).
#
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1        # PowerShell
#   pip install -r requirements.txt
#   python -m ipykernel install --user --name ml-final --display-name "ML-final (core)"
# =============================================================================

# ---- Core ----
numpy
pandas
scipy
scikit-learn
pyarrow

# ---- Visualization ----
matplotlib
seaborn

# ---- Notebooks ----
jupyterlab
ipykernel

# ---- Tree-based models ----
lightgbm
xgboost

# ---- Classical / statistical time series ----
statsmodels
prophet
statsforecast          # Nixtla: fast AutoARIMA / ETS

# ---- Experiment tracking ----
mlflow
wandb
dagshub

# ---- Kaggle data ----
kaggle

# ---- Utilities ----
tqdm
joblib
optuna
python-dotenv

# TIP: the deep-learning stack (torch + neuralforecast) can conflict with
# prophet/statsforecast on some machines. If installs fight each other, put the
# DL libraries in a SEPARATE virtual environment. Once everything installs
# cleanly, freeze exact versions:  pip freeze > requirements.lock.txt

# NOTE: the deep-learning models (N-BEATS, NHITS, DLinear, PatchTST, TFT) are in
# a SEPARATE environment — see requirements-dl.txt. Mixing them here makes pip
# backtrack and silently install a broken neuralforecast 0.1.0.
